var express = require('express'),
    router = express.Router();

router
   // Add a binding for '/tags/automated/'
  .get('/', function(){
    // render the /tags/automated view
  })

module.exports = router;